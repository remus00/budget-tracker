import { DesktopNavbar } from './dektop-navbar';
import { MobileNavbar } from './mobile-navbar';

export const Navbar = () => {
    return (
        <>
            <DesktopNavbar />
            <MobileNavbar />
        </>
    );
};
